# Ferramentas

DETRAN PE: Veículo expõe 1 ferramenta (todas somente leitura).

### 1. `detran_pe_veiculo_consultar`
**Input**: `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional), `placa`, `cpf` (opcional), `cnpj` (opcional)

DETRAN PE: Veículo, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar DETRAN PE: Veículo
```
